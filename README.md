# QCL Labs

A collection of Quantum Computing Lab notebooks, code, and supporting materials.

## Run the notebooks

```sh
uv sync --locked
uv run jupyter lab
```

Open any notebook in the `notebooks` folder and run its cells.
